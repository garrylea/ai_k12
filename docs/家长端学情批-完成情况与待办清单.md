# 家长端学情批 · 完成情况与待办清单

日期：2026-09-19
分支：`feat/parent-insights`（57+ 提交，已合并到 `main` 并推送 origin）
相关文档：设计 `docs/superpowers/specs/2026-09-18-parent-insights-design.md`、实施计划 `docs/superpowers/plans/2026-09-18-parent-insights.md`、历史日志 `docs/ai-core-changelog.md`（2026-09-18 / 2026-09-19 两条）

> **本文件是给「下一段上下文」的入口**：先读它建立全局，再按 §5 的指引去读对应 spec/plan 与代码。

---

## 1. 这批做了什么（已完成）

### 1.1 后端：新模块 `apps/server/src/modules/parent-insights/`

6 个**只读**端点（全部 `@Roles('parent')`，归属校验先于取数，Controller 不手工包信封）：

| 端点 | 说明 |
|---|---|
| `GET /api/parent/dashboard` | 多孩聚合：每孩概览 + 按学科卡片（进度/正确率/自评/未清零错题/考试场次/近 7 天活跃） |
| `GET /api/parent/students/:id/reports?period=weekly\|monthly` | 学情报告，**实时聚合**（不落 `learning_reports`、不调 LLM）：stats/trend/subjects/weakPoints/exams |
| `GET /api/parent/students/:id/errors` | 错题查看（只读主线错题本），7 个筛选 + 分页 |
| `GET /api/parent/students/:id/chat-logs` | AI 对话回放列表，轨道/场景/时间/标题关键词筛选 + 分页 |
| `GET /api/parent/students/:id/chat-logs/:dialogueId` | 单条对话逐句回放（含 `reasoning`、`safetyFlag`） |

内部结构：1 个聚合仓储 `parent-insights.repo.ts`（727 行）+ 4 个 service（`dashboard` / `report` / `errors` / `chat-logs`）+ `dto/` + 两个口径工具 `window.util.ts`（时间窗）/ `rate.util.ts`（正确率，`answered=0` → `null` 而非 `0`）。

**关键口径（改动前先读，别"统一"掉）**：
- 正确率 = `practice_results`（排除 `unanswered`/`self_assess`）+ `exam_answers`（排除 `is_correct IS NULL`）两源合并；窗口可选，**同一套 SQL**。
- 活跃度 = 四表时间并集（`practice_results` / `point_ledger` / `exam_sessions` / `ai_messages`），`lastActiveAt` 全时段、`activeDays7` 近 7 天。
- 错题列表**刻意不 JOIN 知识点**（41% 的题多 KP，JOIN 会让 `LIMIT` 作用在翻倍行上）→ 拆两条查询 + `listErrorKnowledgePoints` 批量取。
- 「已开始学科」= `progress.status <> 'not_started'`（**不是**「有 progress 行」）。
- 会话列表 `updatedAt` = **最后一条消息时间**，不是 `ai_dialogues.updated_at`（后者实质冻结）。

### 1.2 前端：四个页面 + 图表封装

- `pages/parent/`：`ParentDashboardPage` / `ParentReportPage` / `ParentErrorsPage` / `ParentChatLogsPage`（替换四个 `Placeholder`）
- `components/business/parent/`：`chart-theme.ts` + `ChartLine` / `ChartBar`（引入 recharts 3.x，**取色从最近的 `[data-theme]` 容器读**，因 `:root` 是学生端橙色）
- `components/base/Pagination.tsx`（通用分页）
- `services/api.ts` 加 5 个方法 + 类型；`routeTable.tsx` 换 4 个页面、删掉冗余路由 `children-switch`

**两条通则（本批三个页面各踩过一次，已写进根 `CLAUDE.md`）**：
1. **派生状态必须带 `studentId` 归属**——顶栏切孩子不导航、`ParentLayout` 的 `<Outlet />` 无 `key`，页面不重挂载，只按自身维度守卫会在切换首帧画出上一个孩子的数据。`useEffect(reset)` 救不了（effect 在 commit 之后才跑）。
2. **列表页换孩子必须回第 1 页**——否则带「上个孩子的页码」请求新孩子，页数不够时响应回显的 `page` 仍通过「自报家门」守卫，页面停在空态而分页控件只在非空分支渲染，家长无法自救。

### 1.3 数据库：统一 `updated_at` 维护方式

- `tools/db/schema.sql`：30 张表改为**列级** `ON UPDATE CURRENT_TIMESTAMP(3)`；**删除 §14 的 28 条触发器**（全库现在 40 处列级、去重后仅 1 种写法）
- 迁移 `tools/db/migrations/2026-09-19_updated_at_on_update.sql`（幂等），**已应用到 dev 库**
- 原因：触发器是独立对象，schema 与既有库不同步时会静默缺失（实测 28 条只装了 1 条，且长在已废弃的 `aux_error_books` 上）
- 同步：数据库设计文档 §1.2、`install_mysql.sh` 注释、`CLAUDE.md`

### 1.4 文档同步

`openapi.yaml` 与 API 设计文档**逐字段对齐**（含 `Dashboard` schema 重写、删 `reports/{reportId}`、`ErrorItem.source` enum 按实际 6 值、`ChatLogItem.updatedAt` 语义）；UX §5.6、PRD §7.7、changelog、根 `CLAUDE.md`。

### 1.5 测试与验证现状

- 后端 **1166/1166** 通过；`apps/server` 用 `node dist/main.js` 起（不是 `tsx`）
- 前端 **573/573 通过（白天跑）**。⚠️ 但**夜间时段（本地 18:00–06:00）跑会多 1 条红**：
  `routes/routeTable.test.tsx` 里「主轨侧边导航不含辅轨入口」那条，断言 `document.querySelector('[data-theme="student-day"]')` 存在，而 `StudentLayout` 挂载时会 `autoToggleNightMode()` 按挂钟把 `data-theme` 切成 `student-night` → 断言失败（`expected null not to be null`）。
  - **已实测确认**：白天跑 573/573 全绿；`TZ=America/New_York`（当地约 20:45）跑同一文件 → `1 failed | 9 passed`，失败原因正是上面那条。
  - **与本批无关**（本批未碰 `StudentLayout` / `themeStore`），是**既有**问题；但它是**按运行时刻红**，不是「永远红」——**别把它算到新改动头上**，也别当成 flaky。
  - 修法见 §3.3 第 7 项。

---

## 2. 【重点】仪表盘要真正可用，缺的是埋点 —— 需要下一段上下文详细梳理

用户判断：**现在仪表盘显示/统计的内容还不够，这些需要增加大量埋点记录信息后才能真正显示出可用的数据。** 下面是「想展示什么 → 现在有没有数据 → 缺什么」的逐项盘点，作为下一步梳理的起点。

### 2.1 缺的是**学习时长**（最核心，一次解锁四处）

现在仪表盘只有「近 7 天活跃 N 天 + 最近活跃时间」，这是**代理指标**：孩子挂机不答题 = 不活跃；也无法回答「今天学了多久 / 这周比上周多还是少」。

**全库现在没有任何时长字段、没有学习会话表**（`ai_messages.response_time_ms` 恒写 NULL；`progress.last_active_at` 只在首次领课时写一次且仓储不读）。

需要的埋点：一张 **`study_sessions`**（或等价的心跳表），例如 `student_id / subject_id / scene / started_at / last_heartbeat_at / ended_at / active_seconds`，前端在进入学习页、切卡、提交答案、发消息、页面可见性变化时上报（`visibilitychange` + 空闲裁剪）。

**它一次解锁 4 处**：
1. 仪表盘/报告的**学习时长**（PRD §7.7 的原文要求）
2. **每日趋势**（现在只有 `judged_at` 能按天聚合，只覆盖做题；阅读卡片/讨论/背单词完全没数据）
3. **行为管控**的「今日已用时长」（`controls.daily_time_limit_minutes` 只是**上限**，没有已用量）
4. **学习目标**（P6.5）的达成率（`goals` 现在也是死表）

### 2.2 缺 **知识点掌握度**（真薄弱点）

`student_knowledge_mastery` 是**死表**（全仓 `apps/server/src` 零写入）。现在报告的「薄弱知识点」是**错题数代理**，且只有约 40% 的错题能映射到知识点（实测 139 条错题只有 55 条能映射），页面只能靠 `weakPointsUncoveredCount` 提示「另有 N 道未标注」。

需要的埋点：**判题后回写**（答对/答错时更新 `correct_count`/`error_count`/`mastery_score`/`last_seen_at`），落点在 `JudgeCoreService` 的对错出口。顺带要解决**题库 KP 覆盖率**（`question_knowledge_points` 只覆盖 203/529 题）。

### 2.3 缺 **专项类学情**（语文三专项 / 英语背单词 / 辅线）

这三块**都不写 `progress` 行**，所以：
- 仪表盘**没有它们的学科卡**（只背单词的孩子会同时看到「活跃 3 天」和「还没开始任何主线学科的学习」）
- 语文三专项**没有学生进度表**，只能从 `point_ledger`（`ref_type='passage'`）间接推「做过哪些篇目」
- 英语有 `student_word_progress`（`learned`/`wrong_count`）→「共背多少词」「易错词 Top」**可查**；但 `last_seen_at` 是**覆盖式最新值**，「每天背了几个词」**没有数据源**

需要的设计：要么新增按天的专项进度/事件表，要么接受「每天几个词」暂不做。**这一项要先做前置调研再立项。**

### 2.4 已经有数据、够用的部分（不用埋点）

| 想展示 | 数据源 | 状态 |
|---|---|---|
| 进度（到第几单元第几课） | `progress` + 教材链 | ✅ 已用 |
| 正确率（含按天趋势） | `practice_results` + `exam_answers` 的 `judged_at` | ✅ 已用 |
| 未清零错题 / 按知识点聚合 | `main_error_books`(+`qkp`) | ✅ 已用（覆盖率见 2.2） |
| 考试记录/场次 | `exam_sessions` / `exam_answers` | ✅ 已用 |
| 主观题自评 | `question_self_assessments` | ✅ 已用 |
| AI 对话回放（含闲聊标记） | `ai_dialogues` / `ai_messages`(`safety_flag`) | ✅ 已用 |

### 2.5 其它想加但要先定需求的

- **AI 陪伴质量**：讨论轮次、苏格拉底兜底触发次数（`ai_dialogues.consecutive_fail_count` 有值但没按天聚合）
- **异常预警（P6.9）**：`safety_alerts` 表 + 仓储都在，但**没有任何业务代码往里写**；家长端 Banner 目前是**硬编码假数据**（`ParentLayout.tsx` 的 `const hasAlert = true`）。要闭环需在检测侧落库 + 家长端接真数据。

---

## 3. 本批明确留下的后续项

### 3.1 需要裁决 / 先调研（功能性缺口）

| # | 事 | 现状与修法 |
|---|---|---|
| 1 | **家长回放看不到孩子的照片** | 数据在库里、**学生端能看**（`useAuxChat.ts` 把 `attachments` 解析成 `images[]`、`AuxChatPanel` 渲染 `<img>`）；家长端是我写的 8 字段白名单漏了 `attachments`。**照学生端抄**：后端加字段 + DTO/openapi，前端回放页解析 `{type,url}[]` + `resolveAsset` + 破图处理。与 PRD §7.7「全透明回放」是真实缺口 |
| 2 | **专项类学情无学科卡** | 见 §2.3；需前置调研（是否新增按天埋点） |
| 3 | **趋势 X 轴未按窗口铺开** | 相隔 5 天的两次记录在图上是相邻的；柱状图把正确率写进刻度标签（学科多于 4~5 个会挤）。已记 spec §9 为「有意的观感取舍」，可修可留 |
| 4 | **「已开始学科」口径** | 文案已限定为「还没开始任何**主线**学科」，但深层口径（要不要让有专项/答疑产出的学科也出卡）待定 |

### 3.2 打磨类（终审全部判 `accept`，非阻塞）

按性价比排序：

1. **补四个页面的错误态用例**——错题页的 1002 专用卡、回放页的错误态**零用例**（仪表盘/报告页有）
2. **抽 `renderParentPage(path)` 测试助手**——收掉 4 个文件各 ~25 行样板，并收掉「顶栏 `StudentSwitcher` 有同名链接」这个坑（已踩两次，`within(...)` 限定）
3. `PAGE_SIZE = 20` 现有 **4 份拷贝**（`errors.service` / `chat-logs.service` / `points/pagination.util` / `points/redemption.service`）→ 收敛到一处
4. `toDayString` 有两份（`parent-insights.repo.ts` 与 `window.util.ts`）→ 导出一份复用
5. `ParentInsightsRepository` 未进 `database/repositories/index.ts` barrel（其余 10+ 个仓储都在）
6. **拆 `parent-insights.repo.ts`**（727 行，唯一混装四个领域的仓储）——终审明确建议**等下一批碰这块时做**，别在只读批次末尾做纯结构变更
7. 图表 tooltip 的 `background: '#FFFFFF'` 是硬编码色 → 应读 `--bg-card`
8. `Pagination.test.tsx` 第 4 条用例名与断言矛盾（纯改名）
9. a11y：Tab 缺 `aria-controls`/方向键；列表选中项只有颜色、无 `aria-current`
10. `DashboardStudent.schoolLevel` 是**接好但没用**的字段（页面零引用）→ 渲染成学段标签或摘掉；`unreadAlerts` 同（已注明「等预警闭环」）
11. 换孩子时会多发一次被 `cancelled` 丢弃的请求（可在设 `studentId` 的同一处重置 `page` 消除）
12. 错题页的日期范围筛选、以及两页的失败态无用例

### 3.3 仓库既有债（与本批无关，已在 changelog 记名）

1. `GET/PUT /api/parent/students/{id}/subject-configs` 在 API 文档标 MVP 但 **openapi 里没有**（2026-09-01 教材配置批的存量漏收）
2. `openapi.yaml` 里 `HintRequest` **重复定义两次**（YAML 解析静默取后者）
3. 根 `CLAUDE.md` 超 ~15KB 维护目标（本批之前即 20.3KB）
4. `aux_error_books` 废弃表仍在 dev 库（只清了它的触发器，没删表）
5. `tools/db` 有 28 条历史触发器覆盖的表曾被静默漏装 —— **已修**，但同类「schema.sql 与既有库不同步」的风险仍在（无迁移运行器）
6. **（新发现）`routeTable.test.tsx` 那条用例依赖运行时刻**：`StudentLayout` 挂载时 `autoToggleNightMode()` 按挂钟切主题，导致该文件在 18:00–06:00 跑必红（详见 §1.5）。
   - 该文件的 `afterEach` 复位了 `useParentStudentStore`，**没复位 `useThemeStore`**。
   - 建议修法（二选一，都很小）：① 把断言从「`data-theme="student-day"` 存在」改成断言**侧栏/导航存在**（那才是这条用例真正的意图——「宿主在 StudentLayout 下」）；② 或在该用例里 stub 掉 `autoToggleNightMode` / 渲染后固定 `useThemeStore.setState({ mode: 'student-day' })` 再断言。**别只是加 `TZ` 环境变量绕**——那会把时间依赖藏起来。

---

## 4. PRD 里还没做的大块（按缺口大小）

| 模块 | 现状 |
|---|---|
| **主线智能组卷**（单元检测 / 期中 / 期末） | **最大缺口**：`specs/`+`plans/2026-07-23-exam-composition.md` 早已写好但**零代码**；`assessments` / `homeworks` 表 0 引用；前端 `/student/unit-test`、`/exam`、`/scores`、`/homework`、`/homework-result`、`/reward-unlock` 全是 `Placeholder`。PRD §6.1 主线推进靠它，不做主线走不完 |
| 错题变式生成接线 | `VariationCapability` 有、`variation_questions` 表 0 引用、端点/页面全无 |
| 薄弱点图谱与推荐 | `knowledge_relations` / `student_knowledge_mastery` 表 0 引用（与 §2.2 同源） |
| 订阅 / 支付 / 优惠券 / 订单 | 连表都没有（API 文档 §4.15 标 P2） |
| 语音交互 / 口语评测 | 零痕迹 |
| Electron PC App | `apps/desktop` 不存在 |
| 学习时长埋点 / 知识点掌握度写入 | 见 §2.1 / §2.2 |

---

## 5. 下一段上下文怎么开始

**推荐的读法（按顺序）**：

1. 本文件（全局）
2. 你这一轮要动的那块对应的 spec / plan：
   - 家长端学情：`docs/superpowers/specs/2026-09-18-parent-insights-design.md`（**§4.2 端点契约 + §9 已知限制 13 条**是最该读的两节）
   - 实施细节与踩坑：`docs/superpowers/plans/2026-09-18-parent-insights.md`（**Global Constraints 里有两条通则**）
   - 若要动智能组卷：`docs/superpowers/specs/2026-07-23-exam-composition-design.md` + `plans/2026-07-23-exam-composition.md`
3. 历史决策与事故：`docs/ai-core-changelog.md` 顶部两条（2026-09-19 / 2026-09-18）
4. 仍生效的硬约束：根 `CLAUDE.md`（尤其「基本原则」「工程约定」「家长端学情」节）

**若这轮要做 §2 的埋点**：建议先产出一份 spec（`docs/superpowers/specs/YYYY-MM-DD-<topic>-design.md`），把「`study_sessions` 表结构 + 前端上报时机 + 空闲裁剪规则 + 与 `controls`/`goals` 的关系」定下来，再写 plan。**别直接改代码**——这是跨前后端 + 影响既有页面行为的大改。

**动手前的两条环境提醒**：
- 起后端用 `node dist/main.js`（`npx tsx src/main.ts` 的 DI 是坏的）；冒烟验证用**独立端口 + 按 PID 收尾**，别 `pkill -f 'node dist/main.js'`（会杀掉你本机在跑的服务）
- 改 `tools/db/schema.sql` 新增表/列时，**同时**写一份幂等迁移（仓库无迁移运行器，靠手工 apply）
